public class Calculadora {

	public int calcular(int a, String operador, int b) {
		return 0;
/*
		if (operador = "+" != null) {
			return (a+b);
			} else 
		if (operador ="-" != null) {
			return (a-b);
		} else 

		if (operador ="*" != null) {
			return (a*b);
			} else 
		if (operador ="/" != null) {
			return (a/b);
		}
	*/
	}

}
